import { TestePipePipe } from './teste-pipe.pipe';

describe('TestePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TestePipePipe();
    expect(pipe).toBeTruthy();
  });
});
